#include<stdio.h>>
int main()
{
	int kms,m,cm,mm;
	printf("enter the distance in kms");
	scanf("%d",&kms);
	mm=kms*1000*100*10;
	printf("distance betweem two cities in mm is =%d",mm);
	return 0;
}
